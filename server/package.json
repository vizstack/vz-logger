{
  "name": "vizstack-logger-server",
  "dependencies": {
    "dotenv": "^8.0.0",
    "express": "^4.17.1",
    "fs": "0.0.1-security",
    "http": "0.0.0",
    "path": "^0.12.7",
    "socket.io": "^2.2.0",
    "typescript": "^3.5.2"
  },
  "devDependencies": {
    "@types/dotenv": "^6.1.1",
    "@types/express": "^4.17.0",
    "@types/node": "^12.0.8",
    "@types/socket.io": "^2.1.2",
    "nodemon": "^1.19.1",
    "rimraf": "^2.6.3",
    "ts-node": "^8.3.0"
  },
  "scripts": {
    "start": "nodemon --watch 'src/**/*.ts' --exec node --inspect -r ts-node/register src/server.ts",
    "build": "tsc",
    "clean": "rimraf dist/*"
  }
}
